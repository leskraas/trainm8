# Screens

Reference captures at 390 × 872 (iPhone-class logical pixels). The live design file is the
source of truth for anything these images leave ambiguous — open it and click through.

| File | Screen | State shown |
| --- | --- | --- |
| `01-programs.png` | Programs list | StrongLifts running, the other two available; provenance notes visible |
| `02-starting-weights.png` | Starting weights | One field per lift with its published default and hint |
| `03-runner-empty.png` | Runner | Nothing logged yet — target reps shown in every circle |
| `04-runner-logging-rest.png` | Runner | Three squat sets made, warm-up rungs ticked, rest bar counting down |
| `05-runner-missed-set-and-help.png` | Runner | Squat complete, a short bench set in destructive, help panel open |
| `06-what-you-lift-next-time.png` | Outcome | Progress, hold, and the Stall Cut panel that offers nothing |
| `07-lift-over-time.png` | Lift over time | Working weight, est. 1RM, bar chart with the Stall Cut marked, records |

The first screen in the prototype is a "today" view with a bottom tab bar. Both are
prototype scaffolding so the flow can be clicked through — see "Integration" in the main
README for how the section is actually entered.
